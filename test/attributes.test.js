const request = require("supertest");
const baseUrl = "https://ephemeral-manager-governance.sensedia-eng.com";

describe("POST attributes", function(){
    it("Deve criar novo atributo", function(){
        
        return request(baseUrl)
        .post("/api-governance/api/v3/attributes/")
        .set({"Sensedia-Auth":"2e09ad5b-e68f-4f3a-b364-3a5e0668fdd6"})
        .send({
            name:"teste" + Math.floor(Math.random() * 1000),
            tags: ["val1"]
        })
        .expect(201)

    })
    it("Deve criar novo atributo", function(){
        
        return request(baseUrl)
        .post("/api-governance/api/v3/attributes/")
        .set({"Sensedia-Auth":"2e09ad5b-e68f-4f3a-b364-3a5e0668fdd6"})
        .send({
            name:"teste" + Math.floor(Math.random() * 1000),
            tags: ["val1"]
        })
        .expect(201)

    })
    it("Deve criar novo atributo", function(){
        
        return request(baseUrl)
        .post("/api-governance/api/v3/attributes/")
        .set({"Sensedia-Auth":"2e09ad5b-e68f-4f3a-b364-3a5e0668fdd6"})
        .send({
            name:"teste" + Math.floor(Math.random() * 1000),
            tags: ["val1"]
        })
        .expect(201)

    })
    it("Deve criar novo atributo", function(){
        
        return request(baseUrl)
        .post("/api-governance/api/v3/attributes/")
        .set({"Sensedia-Auth":"2e09ad5b-e68f-4f3a-b364-3a5e0668fdd6"})
        .send({
            name:"teste" + Math.floor(Math.random() * 1000),
            tags: ["val1"]
        })
        .expect(201)

    })
    it("Deve criar novo atributo", function(){
        
        return request(baseUrl)
        .post("/api-governance/api/v3/attributes/")
        .set({"Sensedia-Auth":"2e09ad5b-e68f-4f3a-b364-3a5e0668fdd6"})
        .send({
            name:"teste" + Math.floor(Math.random() * 1000),
            tags: ["val1"]
        })
        .expect(201)

    })
    it("Deve criar novo atributo", function(){
        
        return request(baseUrl)
        .post("/api-governance/api/v3/attributes/")
        .set({"Sensedia-Auth":"2e09ad5b-e68f-4f3a-b364-3a5e0668fdd6"})
        .send({
            name:"teste" + Math.floor(Math.random() * 1000),
            tags: ["val1"]
        })
        .expect(201)

    })
    it("Deve criar novo atributo", function(){
        
        return request(baseUrl)
        .post("/api-governance/api/v3/attributes/")
        .set({"Sensedia-Auth":"2e09ad5b-e68f-4f3a-b364-3a5e0668fdd6"})
        .send({
            name:"teste" + Math.floor(Math.random() * 1000),
            tags: ["val1"]
        })
        .expect(201)

    })
    it("Deve criar novo atributo", function(){
        
        return request(baseUrl)
        .post("/api-governance/api/v3/attributes/")
        .set({"Sensedia-Auth":"2e09ad5b-e68f-4f3a-b364-3a5e0668fdd6"})
        .send({
            name:"teste" + Math.floor(Math.random() * 1000),
            tags: ["val1"]
        })
        .expect(201)

    })
    it("Deve criar novo atributo", function(){
        
        return request(baseUrl)
        .post("/api-governance/api/v3/attributes/")
        .set({"Sensedia-Auth":"2e09ad5b-e68f-4f3a-b364-3a5e0668fdd6"})
        .send({
            name:"teste" + Math.floor(Math.random() * 1000),
            tags: ["val1"]
        })
        .expect(201)

    })
    it("Deve criar novo atributo", function(){
        
        return request(baseUrl)
        .post("/api-governance/api/v3/attributes/")
        .set({"Sensedia-Auth":"2e09ad5b-e68f-4f3a-b364-3a5e0668fdd6"})
        .send({
            name:"teste" + Math.floor(Math.random() * 1000),
            tags: ["val1"]
        })
        .expect(201)

    })
    it("Deve criar novo atributo", function(){
        
        return request(baseUrl)
        .post("/api-governance/api/v3/attributes/")
        .set({"Sensedia-Auth":"2e09ad5b-e68f-4f3a-b364-3a5e0668fdd6"})
        .send({
            name:"teste" + Math.floor(Math.random() * 1000),
            tags: ["val1"]
        })
        .expect(201)

    })
    it("Deve criar novo atributo", function(){
        
        return request(baseUrl)
        .post("/api-governance/api/v3/attributes/")
        .set({"Sensedia-Auth":"2e09ad5b-e68f-4f3a-b364-3a5e0668fdd6"})
        .send({
            name:"teste" + Math.floor(Math.random() * 1000),
            tags: ["val1"]
        })
        .expect(201)

    })
    it("Deve criar novo atributo", function(){
        
        return request(baseUrl)
        .post("/api-governance/api/v3/attributes/")
        .set({"Sensedia-Auth":"2e09ad5b-e68f-4f3a-b364-3a5e0668fdd6"})
        .send({
            name:"teste" + Math.floor(Math.random() * 1000),
            tags: ["val1"]
        })
        .expect(201)

    })
    it("Deve criar novo atributo", function(){
        
        return request(baseUrl)
        .post("/api-governance/api/v3/attributes/")
        .set({"Sensedia-Auth":"2e09ad5b-e68f-4f3a-b364-3a5e0668fdd6"})
        .send({
            name:"teste" + Math.floor(Math.random() * 1000),
            tags: ["val1"]
        })
        .expect(201)

    })
    it("Deve criar novo atributo", function(){
        
        return request(baseUrl)
        .post("/api-governance/api/v3/attributes/")
        .set({"Sensedia-Auth":"2e09ad5b-e68f-4f3a-b364-3a5e0668fdd6"})
        .send({
            name:"teste" + Math.floor(Math.random() * 1000),
            tags: ["val1"]
        })
        .expect(201)

    })
    it("Deve criar novo atributo", function(){
        
        return request(baseUrl)
        .post("/api-governance/api/v3/attributes/")
        .set({"Sensedia-Auth":"2e09ad5b-e68f-4f3a-b364-3a5e0668fdd6"})
        .send({
            name:"teste" + Math.floor(Math.random() * 1000),
            tags: ["val1"]
        })
        .expect(201)

    })
    it("Deve criar novo atributo", function(){
        
        return request(baseUrl)
        .post("/api-governance/api/v3/attributes/")
        .set({"Sensedia-Auth":"2e09ad5b-e68f-4f3a-b364-3a5e0668fdd6"})
        .send({
            name:"teste" + Math.floor(Math.random() * 1000),
            tags: ["val1"]
        })
        .expect(201)

    })
    it("Deve criar novo atributo", function(){
        
        return request(baseUrl)
        .post("/api-governance/api/v3/attributes/")
        .set({"Sensedia-Auth":"2e09ad5b-e68f-4f3a-b364-3a5e0668fdd6"})
        .send({
            name:"teste" + Math.floor(Math.random() * 1000),
            tags: ["val1"]
        })
        .expect(201)

    })
    it("Deve criar novo atributo", function(){
        
        return request(baseUrl)
        .post("/api-governance/api/v3/attributes/")
        .set({"Sensedia-Auth":"2e09ad5b-e68f-4f3a-b364-3a5e0668fdd6"})
        .send({
            name:"teste" + Math.floor(Math.random() * 1000),
            tags: ["val1"]
        })
        .expect(201)

    })
    it("Deve criar novo atributo", function(){
        
        return request(baseUrl)
        .post("/api-governance/api/v3/attributes/")
        .set({"Sensedia-Auth":"2e09ad5b-e68f-4f3a-b364-3a5e0668fdd6"})
        .send({
            name:"teste" + Math.floor(Math.random() * 1000),
            tags: ["val1"]
        })
        .expect(201)

    })

})